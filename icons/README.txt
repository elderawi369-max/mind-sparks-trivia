Place your app icons and screenshots here:

REQUIRED ICONS
- icon-192.png         (192x192 PNG)
- icon-512.png         (512x512 PNG)

For best results, use a centered logo on a solid #1a1a2e background.
Note: The manifest lists each icon twice – once with purpose "any" and once with
"maskable" – so they must NOT be combined in a single entry (Chrome requirement).

OPTIONAL SCREENSHOTS (removes richer install UI warnings)
- screenshot-narrow.png  (390x844 PNG) – mobile home screen
- screenshot-wide.png    (1280x800 PNG) – desktop/tablet view

Take real screenshots of the app, or create mockups at those exact pixel sizes.

